services:
  web:
    build: .
    image: music-analyzer-api:1.0
    container_name: fastapi_app
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: uvicorn src.consumer:app --host 0.0.0.0 --port 8000 --reload
    restart: always
